# Built-in-defaults layer, one below CLI/--config/source-profile in precedence.
# Kept mostly empty on purpose -- cctv.config.BUILTIN_DEFAULTS already carries the
# real defaults; this file exists as the documented override point.
